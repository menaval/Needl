<div id="card-box" class= "text-center">
  <%= link_to restaurant_path(restaurant.id), class: "link" do %>
    <h2><%= restaurant.name %></h2>
    <p><%= restaurant.food.name %></p>
    <%= image_tag restaurant.picture_url %>
  <% end %>
</div>
